name: Sync WhenToWork Schedule

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browser
        run: npx playwright install chromium

      - name: Login and export schedule
        env:
          W2W_USERNAME: ${{ secrets.W2W_USERNAME }}
          W2W_PASSWORD: ${{ secrets.W2W_PASSWORD }}
        run: node test-w2w-login.js

      - name: Parse schedule
        run: node parse-w2w-schedule.js

      - name: Sync to MongoDB
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: node sync-w2w-schedule.js
